import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({


    Buttons: {
        color: "#d50102",
        margin: ".5rem",
        fontSize: "1rem",
    },

    buttonHome: {
        color: "white",
        margin: "2rem",
        fontSize: "1.5rem",
    },
}

));
