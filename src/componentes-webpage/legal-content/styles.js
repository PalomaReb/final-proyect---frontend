import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  legalContainer: {
    margin: "2rem auto",
  },
  legalContent: {
    color: "white",
    margin: "8rem 0",
  },
}));
