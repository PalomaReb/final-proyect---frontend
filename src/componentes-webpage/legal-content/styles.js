import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  legalContainer: {
    margin: "0rem auto",
    padding: "2rem",
    minHeight: "80vh"
  },
  legalContent: {
    color: "white",
    margin: "8rem 0",
  },
}));
